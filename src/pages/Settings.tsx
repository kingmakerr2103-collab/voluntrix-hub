import { PlaceholderPage } from "@/components/PlaceholderPage";
import { Settings as SettingsIcon } from "lucide-react";
const Settings = () => (
  <PlaceholderPage title="Settings" description="Account, privacy, notifications and theme." icon={<SettingsIcon className="h-7 w-7" />} />
);
export default Settings;
